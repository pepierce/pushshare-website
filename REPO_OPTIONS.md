# 웹사이트 저장소 관리 옵션 비교

## 📊 방법 비교

| 방법 | 장점 | 단점 | 추천도 |
|------|------|------|--------|
| **1. 웹사이트만 별도 GitHub 저장소** | • 완전 분리<br>• GitHub Pages 설정 간단<br>• 앱 코드는 Bitbucket 유지 | • 두 저장소 관리 필요 | ⭐⭐⭐⭐⭐ |
| **2. 전체 프로젝트를 GitHub에 미러링** | • 한 곳에서 모든 코드 관리<br>• 자동 동기화 | • Bitbucket과 중복<br>• 저장소 크기 증가 | ⭐⭐⭐ |
| **3. 현재 저장소 그대로 + GitHub Pages** | • 구조 변경 없음<br>• 한 저장소로 관리 | • Bitbucket Pages 사용 불가<br>• GitHub에 앱 코드 공개 필요 | ⭐⭐ |

---

## 방법 1: 웹사이트만 별도 GitHub 저장소 (⭐ 추천)

### 설정 방법
자세한 내용은 `SEPARATE_REPO_SETUP.md` 참고

### 요약
```bash
cd website/
git init
git add .
git commit -m "Initial commit"
git remote add origin https://github.com/yourusername/pushshare-website.git
git push -u origin main
```

### 사용 시나리오
- 앱 코드는 Bitbucket에서 계속 관리
- 웹사이트만 GitHub에서 별도 관리
- GitHub Pages로 자동 배포

---

## 방법 2: 전체 프로젝트를 GitHub에 미러링

### 설정 방법

```bash
# 메인 프로젝트 루트로 이동
cd /Users/dosusu/Documents/Personal/PushShare

# GitHub 원격 저장소 추가 (이름: github)
git remote add github https://github.com/yourusername/PushShare.git

# GitHub에 푸시
git push github main

# 이후 Bitbucket에 푸시할 때마다 GitHub에도 푸시
git push origin main    # Bitbucket
git push github main    # GitHub
```

### 자동 동기화 설정

`.git/config` 파일에 다음 추가:
```ini
[remote "github"]
    url = https://github.com/yourusername/PushShare.git
    fetch = +refs/heads/*:refs/remotes/github/*
```

또는 푸시할 때마다:
```bash
git push origin main && git push github main
```

### 사용 시나리오
- Bitbucket과 GitHub 모두에서 코드 관리
- 두 저장소를 동시에 사용하고 싶을 때

---

## 방법 3: 현재 저장소 그대로 + GitHub Pages

### 제약사항
- GitHub Pages는 **같은 저장소**에서만 작동
- Bitbucket 저장소를 GitHub에 미러링해야 함
- 또는 GitHub에 새 저장소 생성 후 코드 복사

### 권장하지 않는 이유
- 앱 코드를 Public으로 공개해야 함 (Private은 유료)
- Bitbucket과 GitHub 중복 관리 필요

---

## 🎯 최종 추천

**방법 1 (웹사이트만 별도 GitHub 저장소)**을 강력 추천합니다:

### 이유
1. ✅ **완전 분리**: 앱 코드와 웹사이트 독립 관리
2. ✅ **간단한 설정**: GitHub Pages 설정이 매우 간단
3. ✅ **보안**: 앱 코드는 Bitbucket에서 Private으로 유지
4. ✅ **유연성**: 웹사이트만 Public으로 공개 가능
5. ✅ **비용**: 완전 무료

### 워크플로우
```
앱 코드 수정 → Bitbucket에 푸시
웹사이트 수정 → GitHub에 푸시 (별도 저장소)
```

---

## 📝 결론

**현재 상황 (Bitbucket 사용 중)에서 가장 좋은 방법:**

1. **웹사이트만 별도 GitHub 저장소로 분리** ⭐
   - `website/` 폴더를 독립적인 Git 저장소로 만들기
   - GitHub에 새 저장소 생성 후 푸시
   - GitHub Pages로 자동 배포

2. **앱 코드는 Bitbucket에서 계속 관리**
   - 기존 워크플로우 유지
   - Private 저장소로 보안 유지

이 방법이 가장 깔끔하고 관리하기 쉽습니다!

